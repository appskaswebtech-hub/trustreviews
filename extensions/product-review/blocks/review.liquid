<div class="review-section">
  <div class="nr-review-top-hed-wr">
    <!-- AVERAGE RATING SUMMARY -->
    <div class="review-summary">
      <div class="summary-inner">
        <div class="nr-review-main-hed">
          <div class="avg-score">
            <span id="avg-rating">0</span><span class="out-of">/5</span>
          </div>
          <div class="avg-stars" id="avg-stars"></div>
        </div>
        <div class="total-reviews"><span id="based-on-label">Based on</span> <span id="total-reviews">0</span> <span id="reviews-word">reviews</span></div>
      </div>
    </div>
    <button id="open-review" class="review-btn">Write a Review</button>
    <div id="review-form" class="review-form" style="display:none">
      <div class="form-header">
        <h3 id="form-title">Write a Review</h3>
        <button class="form-close" id="form-close-btn" aria-label="Close" onclick="closeReview()">✕</button>
      </div>
      <div class="form-field">
        <label><span id="rating-question">What would you rate this product?</span> <span class="required">*</span></label>
        <div class="star-rating" id="star-rating">
          <span data-value="1">★</span>
          <span data-value="2">★</span>
          <span data-value="3">★</span>
          <span data-value="4">★</span>
          <span data-value="5">★</span>
        </div>
      </div>
      <div class="form-field">
        <label id="review-title-label">Review title</label>
        <input type="text" id="review-title" placeholder="Summarize your experience...">
      </div>
      <div class="form-field">
        <label><span id="feedback-label">Tell us your feedback about the product</span> <span class="required">*</span></label>
        <textarea id="comment" placeholder="Share your experience with this product..." required></textarea>
        <div class="form-field">
        <label id="upload-label">Upload image/video</label>
        <input
          type="file"
          id="review-file"
          accept="image/*,video/*"
          onchange="previewReviewFile(event)"
        >
        <div id="file-preview" class="file-preview"></div>
      </div>
      </div>
      <div class="nr-dubble-field">
        <div class="form-field-row">
          <div class="form-field">
            <label><span id="name-label">Your name</span> <span class="required">*</span></label>
            <input type="text" id="name" placeholder="Name" required>
          </div>
        </div>
        <div class="form-field-row">
          <div class="form-field">
            <label><span id="email-label">Your email address</span> <span class="required">*</span></label>
            <input type="email" id="email" placeholder="Email" required>
          </div>
        </div>
      </div>
      <div class="form-actions">
        <button onclick="closeReview()" id="cancel-btn" class="cancel-btn">Cancel</button>
        <button onclick="submitReview()" id="submit-btn" class="submit-btn">Submit</button>
      </div>
    </div>


  </div>
<!-- REVIEWS LIST -->
<div class="reviews-section">
  <div class="reviews-toolbar">
    <div class="toolbar-tabs">
      <span class="tab active" id="reviews-tab-label">Product Reviews</span>
    </div>
    <div class="toolbar-right">

      <!-- SORT DROPDOWN -->
      <div class="sort-wrap">
        <button class="sort-btn" id="sort-btn" onclick="toggleSortMenu()">Sort ▾</button>
        <div class="sort-menu" id="sort-menu" style="display:none">
          <div class="sort-option active" data-sort="newest" onclick="selectSort(this)">Newest First</div>
          <div class="sort-option" data-sort="oldest" onclick="selectSort(this)">Oldest First</div>
          <div class="sort-option" data-sort="highest" onclick="selectSort(this)">Highest Rated</div>
          <div class="sort-option" data-sort="lowest" onclick="selectSort(this)">Lowest Rated</div>
          <div class="sort-option" data-sort="helpful" onclick="selectSort(this)">Most Helpful</div>
        </div>
      </div>

      <!-- SEARCH BOX -->
      <div class="search-wrap">
        <button class="search-btn" id="search-toggle-btn" aria-label="Search reviews" onclick="toggleSearch()">
          <svg viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg"><g id="SVGRepo_bgCarrier" stroke-width="0"></g><g id="SVGRepo_tracerCarrier" stroke-linecap="round" stroke-linejoin="round"></g><g id="SVGRepo_iconCarrier"> <path d="M14.9536 14.9458L21 21M17 10C17 13.866 13.866 17 10 17C6.13401 17 3 13.866 3 10C3 6.13401 6.13401 3 10 3C13.866 3 17 6.13401 17 10Z" stroke="#000000" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path> </g></svg>
          <span id="search-label">Search</span>
        </button>
        <div class="search-box" id="search-box" style="display:none">
          <input type="text" id="search-input" placeholder="Search reviews..." oninput="renderReviews()">
          <button class="search-clear" id="search-clear-btn" aria-label="Clear search" onclick="clearSearch()">✕</button>
        </div>
      </div>

    </div>
  </div>

  <!-- NO RESULTS MESSAGE -->
  <div id="no-results" style="display:none" class="no-results">No reviews match your search.</div>

  <div id="review-list"></div>
</div>

<style>
/* ============ BASE ============ */
.review-section {
  width: 100%;
  max-width: 100%;
  margin: 60px 0;
  padding: 0 40px;
  font-family: var(--rf-font, 'Helvetica Neue', Helvetica, Arial, sans-serif);
  color: #1a1a1a;
}
/* ============ WRITE REVIEW BUTTON ============ */
.review-btn {
  display: inline-block;
  padding: 11px 22px;
  background: var(--rf-accent, #000);
  color: var(--rf-btn-text, #fff);
  border: 1.5px solid var(--rf-accent, #1a1a1a);
  cursor: pointer;
  font-size: 14px;
  font-weight: 600;
  border-radius: var(--rf-radius, 3px);
  letter-spacing: 0.3px;
  transition: background 0.18s, color 0.18s;
}
.review-btn:hover {
  background: var(--rf-accent, #1a1a1a);
  color: var(--rf-btn-text, #fff);
}

/* ============ FORM ============ */
.review-form {
  background: var(--rf-bg, transparent);
  border: 0;
  border-radius: 6px;
  padding: 30px 30px 24px;
  margin-bottom: 32px;
  width: 100%;
}
.form-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 22px;
}
.form-header h3 {
  font-size: 17px;
  font-weight: 700;
  margin: 0;
}
.form-close {
  background: none;
  border: none;
  font-size: 18px;
  cursor: pointer;
  color: #888;
  line-height: 1;
  padding: 0;
}
.form-close:hover { color: #333; }

.form-field-row {
  margin-bottom: 16px;
  width: 100%;
}
@media (max-width: 600px) {
  .form-field-row { grid-template-columns: 1fr; }
}

.form-field {
  display: flex;
  flex-direction: column;
  margin-bottom: 16px;
}
.form-field label {
  font-size: 13px;
  font-weight: 600;
  margin-bottom: 7px;
  color: #333;
}
.required { color: #e53935; }

.form-field input,
.form-field textarea {
  padding: 10px 13px;
  border: 1.5px solid #ddd;
  border-radius: var(--rf-radius, 0px);
  font-size: 14px;
  font-family: inherit;
  color: #1a1a1a;
  background: #fafafa;
  transition: border-color 0.15s;
  outline: none;
  display: flex;
  align-items: center;
}
.form-field input:focus,
.form-field textarea:focus {
  border-color: #1a1a1a;
  background: #fff;
}
.form-field textarea {
  min-height: 110px;
  resize: vertical;
}

/* ============ STAR RATING ============ */
.star-rating {
  display: flex;
  gap: 4px;
  font-size: 30px;
  cursor: pointer;
  margin-top: 4px;
}
.star-rating span {
  color: #ddd;
  transition: color 0.12s, transform 0.1s;
  line-height: 1;
}
.star-rating span.active { color: var(--rf-accent, #000); }
.star-rating span:hover { transform: scale(1.15); }

/* ============ FORM ACTIONS ============ */
.form-actions {
  display: flex;
  justify-content: flex-end;
  gap: 10px;
  margin-top: 20px;
}
.submit-btn {
  padding: 10px 26px;
  background: var(--rf-accent, #1a1a1a);
  color: var(--rf-btn-text, #fff);
  border: none;
  border-radius: var(--rf-radius, 3px);
  font-size: 14px;
  font-weight: 600;
  cursor: pointer;
  transition: background 0.18s;
}
.submit-btn:hover { opacity: 0.88; }
.cancel-btn {
  padding: 10px 20px;
  background: #fff;
  color: #555;
  border: 1.5px solid #ddd;
  border-radius: 3px;
  font-size: 14px;
  cursor: pointer;
  transition: border-color 0.15s;
}
.cancel-btn:hover { border-color: #999; }

/* ============ SUMMARY ============ */
/* .review-summary {
  border: 1.5px solid #e5e5e5;
  border-radius: 6px;
  padding: 24px;
  background: #fff;
  text-align: center;
} */
.avg-score {
  font-size: 38px;
  font-weight: 800;
  line-height: 1;
  color: #1a1a1a;
}
.out-of {
  font-size: 22px;
  font-weight: 400;
  color: #888;
}
.avg-stars {
  font-size: 22px;
  color: #000;
  margin: 7px 0 5px;
}
.total-reviews {
  font-size: 13px;
  color: #888;
}

/* ============ REVIEWS SECTION ============ */
.reviews-section { margin-top: 32px; }
.reviews-toolbar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  border-bottom: 1px solid #000;
  border-top: 1px solid #000;
  padding: 20px 0;
  margin-bottom: 0;
  flex-wrap: wrap;
  gap: 8px;
  flex-direction: row-reverse;
}
.toolbar-tabs { display: flex; gap: 0; }
.tab {
  font-size: 14px;
  font-weight: 600;
  padding: 0px 0px 8px;
  cursor: pointer;
  color: #aaa;
  border-bottom: 2.5px solid transparent;
  margin-bottom: -2px;
  transition: color 0.15s, border-color 0.15s;
}
.tab.active {
  color: #1a1a1a;
  border-bottom-color: #1a1a1a;
}
.toolbar-right {
  display: flex;
  gap: 10px;
  align-items: center;
  padding-bottom: 6px;
  flex-wrap: wrap;
}

/* ============ SORT ============ */
.sort-wrap { position: relative; }
.sort-btn {
  font-size: 16px;
  color: #000000;
  cursor: pointer;
  padding: 0;
  border: 0;
  border-radius: 3px;
  background: transparent;
  transition: background 0.15s;
  white-space: nowrap;
  font-weight: 600;
}
.sort-btn:hover, .sort-btn.open { background: #f0f0f0; border-color: #bbb; }
.sort-menu {
  position: absolute;
  top: calc(100% + 6px);
  left: 0;
  background: #fff;
  border: 1.5px solid #e0e0e0;
  border-radius: 5px;
  box-shadow: 0 4px 16px rgba(0,0,0,0.10);
  z-index: 100;
  min-width: 160px;
  overflow: hidden;
}
.sort-option {
  padding: 10px 16px;
  font-size: 13px;
  color: #333;
  cursor: pointer;
  transition: background 0.12s;
  font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
}
.sort-option:hover { background: #f5f5f5; }
.sort-option.active {
  font-weight: 700;
  color: #1a1a1a;
  background: #f0f0f0;
}

/* ============ SEARCH ============ */
.search-wrap { position: relative; display: flex; align-items: center; gap: 6px; }
.search-btn {
  font-size: 13px;
  color: #555;
  cursor: pointer;
  padding: 6px 12px;
  border: 0;
  border-radius: 3px;
  background: transparent;
  transition: background 0.15s;
  white-space: nowrap;
  display: flex;
  align-items: center;
  gap: 5px;
}
.search-btn:hover, .search-btn.open { background: #f0f0f0; border-color: #bbb; }
.search-box {
  display: flex;
  align-items: center;
  border: 1.5px solid #1a1a1a;
  border-radius: 3px;
  background: #fff;
  overflow: hidden;
  animation: slideIn 0.15s ease;
}
@keyframes slideIn {
  from { opacity: 0; transform: translateX(8px); }
  to   { opacity: 1; transform: translateX(0); }
}
.search-box input {
  border: none;
  outline: none;
  padding: 6px 10px;
  font-size: 13px;
  font-family: inherit;
  color: #1a1a1a;
  width: 180px;
  background: transparent;
}
.search-clear {
  background: none;
  border: none;
  cursor: pointer;
  color: #aaa;
  font-size: 14px;
  padding: 0 8px;
  line-height: 1;
  transition: color 0.12s;
}
.search-clear:hover { color: #1a1a1a; }

/* ============ NO RESULTS ============ */
.no-results {
  padding: 32px 0;
  text-align: center;
  color: #aaa;
  font-size: 14px;
}

/* ============ REVIEW CARD ============ */
.review-card {
  display: flex;
  padding: 24px 0;
  border-bottom: 1px solid #000;
  align-items: flex-start;
}
.review-avatar {
  flex-shrink: 0;
  width: 60px;
  height: 60px;
  background: #0000001a;
  color: #000;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 15px;
  font-weight: 700;
  letter-spacing: 0.5px;
  padding-right: 0px;
}
.review-avatar-outer{
  margin-right: 20px;
  width: 15%;
}
.review-body { 
  flex: 1; 
  min-width: 0; 
  padding-left: 20px;
  border-left: 1px solid #000;
  width: 85%;
}
.review-stars {
  font-size: 17px;
  color: #000;
  margin-bottom: 4px;
  display: flex;
  gap: 2px;
}
.review-stars .dim { color: #ddd; }
.review-product-label {
  font-size: 13px;
  font-weight: 600;
  color: #1a1a1a;
  margin-bottom: 4px;
}
.review-author {
  font-size: 13px;
  color: #888;
  margin-bottom: 8px;
}
.review-comment {
  font-size: 14px;
  color: #333;
  line-height: 1.6;
  margin: 0 0 12px;
}
.review-meta {
  display: flex;
  align-items: center;
  gap: 14px;
  flex-wrap: wrap;
}
.helpful-btn {
  display: flex;
  align-items: center;
  gap: 5px;
  background: none;
  border: 1.2px solid #ddd;
  border-radius: 3px;
  padding: 5px 12px;
  font-size: 12.5px;
  cursor: pointer;
  color: #555;
  transition: border-color 0.15s, background 0.15s;
}
.helpful-btn:hover { border-color: #1a1a1a; background: #f7f7f7; }
.helpful-btn .check { color: #2e7d32; font-weight: 700; }
.share-btn {
  background: none;
  border: none;
  font-size: 12.5px;
  color: #888;
  cursor: pointer;
  padding: 5px 0;
  text-decoration: underline;
  text-underline-offset: 2px;
}
.share-btn:hover { color: #1a1a1a; }

/* search highlight */
.highlight {
  background: #fff3cd;
  border-radius: 2px;
  padding: 0 1px;
}

.nr-review-top-hed-wr {
  display: flex;
  /* flex-direction: row-reverse; */
  justify-content: space-between;
  align-items: center;
  flex-wrap: wrap;
}
.nr-review-main-hed {
  display: flex;
  gap: 10px;
  align-items: center;
}
.review-summary .summary-inner {
  display: flex;
  flex-direction: column;
  gap: 10px;
}
.search-btn svg {
  height: 20px;
  width: 20px;
}
.nr-dubble-field {
  display: flex;
  gap: 10px;
}
@media (max-width: 991px) {
  .review-section { 
    padding: 0; 
  }
  .review-card { 
    flex-direction: column; 
    gap: 10px;
  }
  .review-body{
    border-left: 0;
    width: 100%;
    padding-left: 0;
  }
  .reviews-toolbar {
    flex-direction: column-reverse;
    gap: 8px;
    border: 0;
    align-items: start;
    padding-bottom: 0;
  }
  .toolbar-right {
    margin-bottom: 6px;
    width: 100%;
    border-top: 1px solid #000;
    border-bottom: 1px solid #000;
    padding: 10px 0;
  }
  .search-box input { 
    width: 130px; 
  }
  .toolbar-tabs {
    margin: 0 auto;
  }
  .nr-review-top-hed-wr {
    gap: 10px;
    align-items: start;
  }
  .nr-review-top-hed-wr .review-btn {
    width: 100%;
  }
  .nr-dubble-field {
    flex-direction: column;
  }
  .review-form{
    padding: 30px 0 24px;
  }
  .form-field-row {
    margin-bottom: 0px;
  }
  .form-actions {
    flex-direction: column;
    margin-top: 10px;
  }
}
  .file-preview {
    margin-top: 10px;
  }

  .file-preview img,
  .file-preview video {
    max-width: 120px;
    border: 1px solid #ddd;
    border-radius: 4px;
  }

  /* ============ REVIEW MEDIA ============ */
.review-media {
  margin: 12px 0;
  border-radius: 6px;
  overflow: hidden;
}

.review-media img {
  max-width: 100%;
  width: auto;
  max-height: 100px;
  border-radius: 4px;
  border: 1px solid #e0e0e0;
  cursor: pointer;
  transition: opacity 0.2s;
  display: block;
}

.review-media img:hover {
  opacity: 0.9;
}

.review-media video {
  max-width: 100%;
  width: 100%;
  max-height: 122px;
  border-radius: 4px;
  border: 1px solid #e0e0e0;
  display: block;
}

/* ============ MEDIA MODAL (for full-size image view) ============ */
.media-modal {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(0, 0, 0, 0.9);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 10000;
  animation: fadeIn 0.2s ease;
}

@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

.media-modal-content {
  position: relative;
  max-width: 90%;
  max-height: 90%;
  display: flex;
  align-items: center;
  justify-content: center;
}

.media-modal-content img {
  max-width: 100%;
  max-height: 90vh;
  border-radius: 4px;
  box-shadow: 0 4px 20px rgba(0, 0, 0, 0.5);
}

.media-modal-close {
  position: absolute;
  top: -40px;
  right: 0;
  font-size: 36px;
  color: #fff;
  cursor: pointer;
  font-weight: 300;
  line-height: 1;
  transition: color 0.2s;
}

.media-modal-close:hover {
  color: #ccc;
}

/* Mobile adjustments */
@media (max-width: 991px) {
  .review-media img {
    max-height: 250px;
  }

  .review-media video {
    max-height: 300px;
  }

  .media-modal-content {
    max-width: 95%;
    max-height: 95%;
  }

  .media-modal-close {
    top: -35px;
    font-size: 32px;
  }
}
.flex-body {
    display: flex;
    justify-content: space-between;
}
</style>

<script>
const productId = "{{ product.id }}";
const productTitle = {{ product.title | json }};
const storeLocale = {{ request.locale.iso_code | json }};
let rating = 0;
let allReviews = [];
let currentSort = "newest";
let searchQuery = "";

/* ============ TRANSLATIONS ============ */
// Fallback only — the full set of languages is served by /apps/review (see
// app/utils/widgetTranslations.server.js) to stay under Shopify's 100 KB
// Liquid-content cap per theme app extension.
const REVIEW_TRANSLATIONS_EN = {
  basedOn: "Based on", reviewsWord: "reviews", writeReview: "Write a Review", close: "Close",
  ratingQuestion: "What would you rate this product?", reviewTitleLabel: "Review title",
  reviewTitlePlaceholder: "Summarize your experience...",
  feedbackLabel: "Tell us your feedback about the product",
  feedbackPlaceholder: "Share your experience with this product...",
  uploadLabel: "Upload image/video", nameLabel: "Your name", namePlaceholder: "Name",
  emailLabel: "Your email address", emailPlaceholder: "Email", cancel: "Cancel", submit: "Submit",
  productReviewsTab: "Product Reviews", sort: "Sort",
  sortOptions: { newest: "Newest First", oldest: "Oldest First", highest: "Highest Rated", lowest: "Lowest Rated", helpful: "Most Helpful" },
  search: "Search", searchAria: "Search reviews", searchPlaceholder: "Search reviews...",
  searchClearAria: "Clear search", noResults: "No reviews match your search.",
  helpful: "Helpful", share: "Share",
  requiredFieldsAlert: "Please fill all required fields", reviewLinkCopiedAlert: "Review link copied!",
};
let T = REVIEW_TRANSLATIONS_EN;

function applyTranslations(remoteT){
  T = remoteT || REVIEW_TRANSLATIONS_EN;

  document.getElementById("based-on-label").textContent = T.basedOn;
  document.getElementById("reviews-word").textContent = T.reviewsWord;
  document.getElementById("open-review").textContent = T.writeReview;
  document.getElementById("form-title").textContent = T.writeReview;
  document.getElementById("form-close-btn").setAttribute("aria-label", T.close);
  document.getElementById("rating-question").textContent = T.ratingQuestion;
  document.getElementById("review-title-label").textContent = T.reviewTitleLabel;
  document.getElementById("review-title").placeholder = T.reviewTitlePlaceholder;
  document.getElementById("feedback-label").textContent = T.feedbackLabel;
  document.getElementById("comment").placeholder = T.feedbackPlaceholder;
  document.getElementById("upload-label").textContent = T.uploadLabel;
  document.getElementById("name-label").textContent = T.nameLabel;
  document.getElementById("name").placeholder = T.namePlaceholder;
  document.getElementById("email-label").textContent = T.emailLabel;
  document.getElementById("email").placeholder = T.emailPlaceholder;
  document.getElementById("cancel-btn").textContent = T.cancel;
  document.getElementById("submit-btn").textContent = T.submit;
  document.getElementById("reviews-tab-label").textContent = T.productReviewsTab;
  document.getElementById("sort-btn").textContent = T.sort + " ▾";
  document.querySelectorAll(".sort-option").forEach(function(el){
    var key = el.dataset.sort;
    if (T.sortOptions[key]) el.textContent = T.sortOptions[key];
  });
  document.getElementById("search-toggle-btn").setAttribute("aria-label", T.searchAria);
  document.getElementById("search-label").textContent = T.search;
  document.getElementById("search-input").placeholder = T.searchPlaceholder;
  document.getElementById("search-clear-btn").setAttribute("aria-label", T.searchClearAria);
  document.getElementById("no-results").textContent = T.noResults;
}

/* ============ OPEN / CLOSE FORM ============ */
document.getElementById("open-review").onclick = function(){
  document.getElementById("review-form").style.display = "block";
  this.style.display = "none";
};
function closeReview(){
  document.getElementById("review-form").style.display = "none";
  document.getElementById("open-review").style.display = "inline-block";
}

/* ============ STAR CLICK ============ */
document.querySelectorAll("#star-rating span").forEach(star => {
  star.addEventListener("click", function(){
    rating = this.dataset.value;
    document.querySelectorAll("#star-rating span").forEach((s, i) => {
      s.classList.toggle("active", i < rating);
    });
  });
  star.addEventListener("mouseover", function(){
    const hv = this.dataset.value;
    document.querySelectorAll("#star-rating span").forEach((s, i) => {
      s.classList.toggle("active", i < hv);
    });
  });
  star.addEventListener("mouseout", function(){
    document.querySelectorAll("#star-rating span").forEach((s, i) => {
      s.classList.toggle("active", i < rating);
    });
  });
});

function previewReviewFile(event) {
  const file = event.target.files[0];
  const preview = document.getElementById("file-preview");

  preview.innerHTML = "";

  if (!file) return;

  // if (file.size > 5 * 1024 * 1024) {
  //   alert("File size must be under 5MB");
  //   event.target.value = "";
  //   return;
  // }

  const url = URL.createObjectURL(file);

  if (file.type.startsWith("image/")) {
    preview.innerHTML = `<img src="${url}" alt="Review upload">`;
  } else if (file.type.startsWith("video/")) {
    preview.innerHTML = `<video src="${url}" controls></video>`;
  }
}

/* ============ SORT MENU ============ */
function toggleSortMenu(){
  const menu = document.getElementById("sort-menu");
  const btn  = document.getElementById("sort-btn");
  const isOpen = menu.style.display !== "none";
  menu.style.display = isOpen ? "none" : "block";
  btn.classList.toggle("open", !isOpen);
}
function selectSort(el){
  document.querySelectorAll(".sort-option").forEach(o => o.classList.remove("active"));
  el.classList.add("active");
  currentSort = el.dataset.sort;
  document.getElementById("sort-menu").style.display = "none";
  document.getElementById("sort-btn").classList.remove("open");
  document.getElementById("sort-btn").textContent = el.textContent + " ▾";
  renderReviews();
}
document.addEventListener("click", function(e){
  if(!e.target.closest(".sort-wrap")){
    document.getElementById("sort-menu").style.display = "none";
    document.getElementById("sort-btn").classList.remove("open");
  }
});

/* ============ SEARCH ============ */
function toggleSearch(){
  const box = document.getElementById("search-box");
  const btn = document.getElementById("search-toggle-btn");
  const isOpen = box.style.display !== "none";
  box.style.display = isOpen ? "none" : "flex";
  btn.classList.toggle("open", !isOpen);
  if(!isOpen) document.getElementById("search-input").focus();
}
function clearSearch(){
  document.getElementById("search-input").value = "";
  searchQuery = "";
  renderReviews();
  document.getElementById("search-input").focus();
}
document.addEventListener("input", function(e){
  if(e.target.id === "search-input"){
    searchQuery = e.target.value.trim().toLowerCase();
    renderReviews();
  }
});

/* ============ INITIALS ============ */
function getInitials(name){
  return (name || "?").trim().split(" ").map(w => w[0]).join("").toUpperCase().slice(0,2) || "?";
}

function escapeHTML(text){
  return String(text || "").replace(/[&<>"']/g, function(char){
    return {
      "&": "&amp;",
      "<": "&lt;",
      ">": "&gt;",
      "\"": "&quot;",
      "'": "&#39;"
    }[char];
  });
}

/* ============ HIGHLIGHT SEARCH TERM ============ */
function highlight(text, query){
  const safeText = escapeHTML(text);
  if(!query) return safeText;
  const escaped = query.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
  return safeText.replace(new RegExp("(" + escaped + ")", "gi"), '<span class="highlight">$1</span>');
}

/* ============ RENDER REVIEWS (sort + search) ============ */
function renderReviews(){
  const container = document.getElementById("review-list");
  const noResults = document.getElementById("no-results");
  const q = searchQuery;

  let filtered = allReviews.filter(r => {
    if(!q) return true;
    return (r.customer || "").toLowerCase().includes(q) ||
           (r.comment  || "").toLowerCase().includes(q);
  });

  filtered = filtered.slice().sort((a, b) => {
    if(currentSort === "newest")  return new Date(b.createdAt || 0) - new Date(a.createdAt || 0);
    if(currentSort === "oldest")  return new Date(a.createdAt || 0) - new Date(b.createdAt || 0);
    if(currentSort === "highest") return b.rating - a.rating;
    if(currentSort === "lowest")  return a.rating - b.rating;
    if(currentSort === "helpful") return (b.likes || 0) - (a.likes || 0);
    return 0;
  });

  if(filtered.length === 0){
    container.innerHTML = "";
    noResults.style.display = "block";
    return;
  }
  noResults.style.display = "none";
  container.innerHTML = "";

  filtered.forEach(r => {
    let stars = "";
    for(let i = 1; i <= 5; i++){
      stars += i <= r.rating ? '<span>★</span>' : '<span class="dim">★</span>';
    }
    const initials      = getInitials(r.customer);
    const customerHTML  = highlight(r.customer || "", q);
    const commentHTML   = highlight(r.comment  || "", q);
    const productLabel  = escapeHTML(productTitle);

    let mediaHTML = "";
    if (r.mediaUrl) {
      if (r.mediaType && r.mediaType.startsWith("image/")) {
        mediaHTML = `
          <div class="review-media">
            <img src="https://trustreviews.kaswebtechsolutions.com${escapeHTML(r.mediaUrl)}" alt="Review image" onclick="openMediaModal('https://staging-trustreviews.kaswebtechsolutions.com${escapeHTML(r.mediaUrl)}', 'image')" />
          </div>
        `;
      } else if (r.mediaType && r.mediaType.startsWith("video/")) {
        mediaHTML = `
          <div class="review-media">
            <video src="https://trustreviews.kaswebtechsolutions.com${escapeHTML(r.mediaUrl)}" controls></video>
          </div>
        `;
      }
    }

    container.innerHTML += `
      <div class="review-card" id="review-${r.id}">
        <div class="review-avatar-outer">
          <div class="review-avatar">${initials}</div>
        </div>
        <div class="review-body">

          <div class="flex-body">
          <div class="review-flex">
          <div class="review-stars">${stars}</div>
          <div class="review-product-label">${productLabel}</div>
          <div class="review-author">${customerHTML}</div>
          <p class="review-comment">${commentHTML}</p>
          </div>
          ${mediaHTML}
          </div>
          <div class="review-meta">
            <button class="helpful-btn" onclick="likeReview(${r.id})">
              <span class="check">✓</span> ${T.helpful} (${r.likes || 0})
            </button>
            <button class="share-btn" onclick="shareReview(${r.id})">${T.share}</button>
          </div>
        </div>
      </div>
    `;
  });
}

/* ============ MEDIA MODAL ============ */
function openMediaModal(url, type) {
  const modal = document.createElement('div');
  modal.className = 'media-modal';
  modal.onclick = function() {
    document.body.removeChild(modal);
  };

  if (type === 'image') {
    modal.innerHTML = `
      <div class="media-modal-content">
        <span class="media-modal-close">&times;</span>
        <img src="${url}" alt="Review image full size" />
      </div>
    `;
  }

  document.body.appendChild(modal);
}

/* ============ FETCH REVIEWS ============ */
async function loadReviews(){
  try {
    const res  = await fetch(`/apps/review?productId=${encodeURIComponent(productId)}&locale=${encodeURIComponent(storeLocale)}`);
    if(!res.ok) throw new Error("Unable to load reviews");
    const data = await res.json();
    allReviews = data.reviews || [];
    applyTranslations(data.translations);

    const total = Number(data.total || 0);
    const avgValue = Number(data.averageRating || 0);
    const avg = total ? avgValue.toFixed(1) : "0.0";
    document.getElementById("avg-rating").innerText    = avg;
    document.getElementById("total-reviews").innerText = total;

    let avgStars = "";
    for(let i = 1; i <= 5; i++){
      avgStars += i <= Math.round(avgValue)
        ? '<span>★</span>'
        : '<span style="color:#ddd">★</span>';
    }
    document.getElementById("avg-stars").innerHTML = avgStars;

    renderReviews();
  } catch(e){
    console.error("loadReviews error:", e);
  }
}

/* ============ SUBMIT REVIEW ============ */
async function submitReview() {
  const name = document.getElementById("name").value.trim();
  const email = document.getElementById("email").value.trim();
  const comment = document.getElementById("comment").value.trim();
  const file = document.getElementById("review-file")?.files[0];
  const title   = document.getElementById("review-title").value.trim(); 

  if (!name || !email || !comment || rating == 0) {
    alert(T.requiredFieldsAlert);
    return;
  }

  let uploadedFile = null;

  try {
    if (file) {
      const uploadFormData = new FormData();
      uploadFormData.append("file", file);

      const uploadResponse = await fetch("/apps/review", {
        method: "POST",
        body: uploadFormData
      });

      uploadedFile = await uploadResponse.json();

      if (!uploadedFile.success) {
        throw new Error("File upload failed");
      }
    }

    const data = {
      email,
      productId,
      rating,
      comment,
      title,          // ← NEW — sent to backend
      customer: name,
      mediaUrl: uploadedFile?.url || null,
      mediaType: uploadedFile?.mediaType || null,
      fileName: uploadedFile?.fileName || null
    };

    const response = await fetch("/apps/review", {
      method: "POST",
      headers: {
        "Content-Type": "application/json"
      },
      body: JSON.stringify(data)
    });

    if (!response.ok) {
      throw new Error("Review submit failed");
    }

    document.getElementById("name").value = "";
    document.getElementById("email").value = "";
    document.getElementById("comment").value = "";
    document.getElementById("review-file").value = "";
    document.getElementById("file-preview").innerHTML = "";

    rating = 0;

    document.querySelectorAll("#star-rating span").forEach(s => {
      s.classList.remove("active");
    });

    closeReview();
    loadReviews();

  } catch (err) {
    console.error(err);
    alert(err.message);
  }
}

/* ============ LIKE REVIEW ============ */
async function likeReview(id){
  const response = await fetch("/apps/review", {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ type: "like", id: id })
  });
  if(!response.ok) return;
  loadReviews();
}

/* ============ SHARE REVIEW ============ */
function shareReview(id){
  const url = window.location.href + "#review-" + id;
  navigator.clipboard.writeText(url);
  alert(T.reviewLinkCopiedAlert);
}

/* ============ FORM STYLE (color/font/size/background) ============ */
async function applyFormStyle(){
  try {
    const res = await fetch("/apps/review?type=form-settings");
    if(!res.ok) return;
    const data = await res.json();
    const s = data.settings || {};
    const section = document.querySelector(".review-section");
    if(!section) return;
    section.style.setProperty("--rf-accent",   s.accentColor     || "#1a1a1a");
    section.style.setProperty("--rf-btn-text", s.buttonTextColor || "#FFFFFF");
    section.style.setProperty("--rf-bg",       s.backgroundColor || "transparent");
    section.style.setProperty("--rf-font",     s.fontFamily      || "inherit");
    section.style.setProperty("--rf-radius",   (s.borderRadius ?? 3) + "px");
  } catch(e){
    console.error("Review form style error:", e);
  }
}

/* ============ INIT ============ */
loadReviews();
applyFormStyle();
</script>

{% schema %}
{
  "name": "Product Review",
  "target": "section",
  "settings": []
}
{% endschema %}
</div>

